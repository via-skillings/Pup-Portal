<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pup Portal</title>
  <link rel="stylesheet" href="./css/style.css"/>
</head>
<body>
  <header>
    <h1>
      Pup Portal
    </h1>
    <!--href connecting to homepage homeRoute, which will render homepage handlebars view -->
    <a href="/">Home Page</a>
    <!--if logged in render the logout button in the header, else render login button-->
    {{#if logged_in}}
        <button>Logout</button>
        <script src="./js/logout.js"></script>
      {{else}}
      <!--href connecting to login homeRoute, which will render login handlebars view -->
        <a href="/login">Login</a> 
      {{/if}}
  </header>
  <main>
    {{{ body }}}
  </main>
  <footer></footer>
</body>

</html>
